---
"@ifc-lite/lens": minor
---

**lens**: new built-in `By Stage` lens (`lens-by-stage`) that auto-colors entities by the `CESIUM.Stage` property — one color per distinct stage value — so construction-stage tags written in the viewer are immediately visible.
